# Адаптация сайта под мобильные устройства

## Выполненные изменения

### 1. Header (Шапка сайта)
**Изменения:**
- Добавлено мобильное бургер-меню для экранов < 1024px
- Кнопка "Оставить заявку" перенесена в мобильное меню (скрыта в шапке на мобильных)
- Логотип: единый размер `h-10` (40px) на всех устройствах
- Контакты скрыты на мобильных (`hidden md:flex`), отображаются в мобильном меню
- Выдвижное меню с анимацией `animate-slide-in`
- Раскрывающиеся подменю для "Краны" и "Каталог"

**Брейкпоинты:**
- `< 1024px` - мобильное меню с бургером
- `≥ 1024px` - десктопное горизонтальное меню

---

### 2. Hero секция (Главный баннер)
**Изменения:**
- **Высота секции (видео):**
  - Мобильные: `h-[450px]` (уменьшено с 600px)
  - Планшеты: `h-[500px]`
  - Десктоп: `h-[600px]`

- **Заголовок:**
  - Адаптивные размеры: `text-3xl sm:text-4xl md:text-5xl lg:text-6xl`
  - Плавное масштабирование

- **УТП карточки (уменьшены в 2+ раза на мобильных):**
  - Gap: `gap-2 sm:gap-4 md:gap-6` (8px → 16px → 24px)
  - Padding: `py-2 px-1.5 sm:py-4 sm:px-3` (уменьшен в 2 раза)
  - Высота цифр: `h-8 sm:h-14 md:h-16` (32px → 56px → 64px)
  - Размер цифр: `text-xl sm:text-4xl md:text-5xl` (20px → 36px → 48px)
  - Размер иконок: `w-8 h-8 sm:w-14 sm:h-14 md:w-16 md:h-16`
  - Размер текста: `text-[8px] sm:text-xs` (8px → 12px)
  - Отступы: `mb-0.5 sm:mb-2` (2px → 8px)
  - Отступ снизу: `pb-3 sm:pb-6 md:pb-8`

**Брейкпоинты:**
- `< 640px` - компактные карточки
- `640px - 768px` - средние карточки
- `≥ 768px` - полноразмерные карточки

---

### 3. Footer (Подвал)
**Изменения:**
- **Сетка на мобильных:** `grid-cols-2` (2 колонки вместо 1)
- **Логотип и контакты:** `col-span-2` - занимают всю ширину сверху
- **Центрирование на мобильных:** `text-center sm:text-left`
- **Логотип:** `mx-auto sm:mx-0` - по центру на мобильных
- **Адаптивные размеры:**
  - Логотип: `h-10 sm:h-12`
  - Телефон: `text-base sm:text-lg`
  - Email: `text-xs sm:text-sm`
  - Заголовки: `text-sm`
  - Ссылки: `text-xs sm:text-sm`
- **Отступы:** `py-8 sm:py-12`, `gap-6 sm:gap-8`
- **Нижние ссылки:** вертикально на мобильных `flex-col sm:flex-row`

**Структура на мобильных:**
```
[Логотип + Контакты - 2 колонки]
[Навигация] [Документы]
[Copyright и ссылки]
```

**Брейкпоинты:**
- `< 640px` - 2 колонки
- `640px - 1024px` - 2 колонки с увеличенными размерами
- `≥ 1024px` - 4 колонки (десктоп)

---

### 4. Глобальные стили (index.css)
**Изменения:**
- Добавлен `overflow-x: hidden` для `html` и `body`
- Добавлен `width: 100%` и `max-width: 100vw` для предотвращения горизонтального скролла
- Правила для `#root` с ограничениями ширины
- Анимация `slide-in` для мобильного меню

---

### 5. App.jsx
**Изменения:**
- Добавлены классы `overflow-x-hidden w-full max-w-full` для главного контейнера
- Предотвращение горизонтального скролла на всех уровнях

---

## Брейкпоинты Tailwind CSS

- **sm**: 640px и выше (планшеты портрет)
- **md**: 768px и выше (планшеты ландшафт)
- **lg**: 1024px и выше (ноутбуки)
- **xl**: 1280px и выше (десктопы)

---

## Тестирование

Рекомендуется протестировать на:
- 📱 320px - 480px (мобильные телефоны)
- 📱 481px - 768px (планшеты портрет)
- 💻 769px - 1024px (планшеты ландшафт)
- 🖥️ 1025px+ (десктопы)

---

